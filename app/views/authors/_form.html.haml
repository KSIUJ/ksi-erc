= form_for @author do |f|
  - if @author.errors.any?
    #error_explanation
      %h2= "Nie można zapisać autora z powodu następujących błędów:"
      %ul
        - @author.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Zapisz'
