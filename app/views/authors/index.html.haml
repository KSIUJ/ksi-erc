= link_to 'Nowy autor', new_author_path, class: "btn btn-default pull-right"
%h1 Lista autorów

%table#authors
  %thead
    %tr
      %th Nazwisko
      %th
      %th
      %th

  %tbody
    - @authors.each do |author|
      %tr
        %td= author.name
        %td= link_to 'Pokaż', author
        %td= link_to 'Edytuj', edit_author_path(author)
        %td= link_to 'Usuń', author, :method => :delete, :data => { :confirm => 'Czy na pewno?' }