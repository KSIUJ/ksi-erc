= form_for @publishing_house do |f|
  - if @publishing_house.errors.any?
    #error_explanation
      %h2= "#{pluralize(@publishing_house.errors.count, "error")} prohibited this publishing_house from being saved:"
      %ul
        - @publishing_house.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
